to set up your environment

make sure you have conda installed: [install](https://docs.conda.io/projects/conda/en/stable/user-guide/install/linux.html)

create virtual env
```bash
conda env create --file=environment.yaml
```
activate virtual env
```bash
conda activate pyllm
```